name: Tests

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
      - name: Install dependencies
        working-directory: functions
        run: npm ci
      - name: Debug Prettier formatting
        working-directory: functions
        run: |
          npm run format
          git --no-pager diff -- index.js
          git diff --exit-code -- index.js

      - name: Run unit, frontend, and OpenAPI contract tests
        working-directory: functions
        run: npm test
