rules_version = '2';

// Local emulator only — this project's Cloud Functions (using firebase-admin,
// which bypasses these rules) are the actual production data-access path.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
